package edu.ntnu.trym.simulation;

import edu.ntnu.trym.simulation.controller.MainMenuController;

/**
 * This class acts as the starting point for the whole application.
 */
public class SimulationApplication {

    /**
     * The method that is called during run-time to start the applications.
     * @param args  The arguments that are used to launch the application.
     */
    public static void main(String[] args) {
        MainMenuController.main(args);
    }
}
